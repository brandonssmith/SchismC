I64 add(I64 a, I64 b) {
    return a + b;
}

I64 multiply(I64 x, I64 y) {
    return x * y;
}

I64 subtract(I64 p, I64 q) {
    return p - q;
}

add(5, 10);
multiply(3, 4);
subtract(20, 7);
